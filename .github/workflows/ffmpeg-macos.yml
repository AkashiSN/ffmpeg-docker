name: ffmpeg-macos

on:
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-26
    if: github.ref == 'refs/heads/main' && startsWith(github.ref, 'refs/tags/') == false
    timeout-minutes: 120

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build FFmpeg library
        run: ./scripts/macos/build-library.sh

      - name: Build FFmpeg
        run: ./scripts/macos/build-ffmpeg.sh

      - name: Verify build
        run: |
          /tmp/dist/bin/ffmpeg -version
          /tmp/dist/bin/ffprobe -version

      - name: Upload FFmpeg binaries
        uses: actions/upload-artifact@v4
        with:
          name: ffmpeg-macos-arm64
          path: |
            /tmp/dist/bin/ffmpeg
            /tmp/dist/bin/ffprobe
          retention-days: 30

      - name: Create build info
        run: |
          echo "Build Date: $(date)" > /tmp/dist/bin/build-info.txt
          echo "Runner: ${{ runner.os }}-${{ runner.arch }}" >> /tmp/dist/bin/build-info.txt
          echo "Commit: ${{ github.sha }}" >> /tmp/dist/bin/build-info.txt
          /tmp/dist/bin/ffmpeg -version >> /tmp/dist/bin/build-info.txt

      - name: Upload build info
        uses: actions/upload-artifact@v4
        with:
          name: build-info
          path: /tmp/dist/bin/build-info.txt
          retention-days: 30
