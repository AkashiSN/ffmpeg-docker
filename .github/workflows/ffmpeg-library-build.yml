name: ffmpeg-library-build

on:
  push:
    paths:
      - 'library.Dockerfile'
      - '.github/workflows/ffmpeg-library-build.yml'
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/') == false
    strategy:
      matrix:
        os: [linux, windows]
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Cache Docker layers
        uses: actions/cache@v2
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-${{ github.workflow }}-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-${{ github.workflow }}-

      - name: Login to GHCR
        uses: docker/login-action@v1
        with:
          registry: ghcr.io
          username: akashisn
          password: ${{ github.token }}

      - name: Set build target (windows)
        if: matrix.os == 'windows'
        run: |
          echo "host_target=x86_64-w64-mingw32" >> $GITHUB_ENV

      - name: Build ffmpeg-build-base
        uses: docker/build-push-action@v2
        with:
          context: ./
          file: ./library.Dockerfile
          tags: |
            ghcr.io/akashisn/ffmpeg-library-build:${{ matrix.os }}
          build-args: |
            HOST_TARGET=${{ env.host_target }}
          platforms: linux/amd64
          push: true
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      - name: Move cache # https://github.com/docker/build-push-action/issues/252
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache
